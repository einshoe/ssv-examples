// For actions related to the detailed view (These are actually mostly sitting in Stores/UI)
import AppDispatcher from "../AppDispatcher";

const DetailedActionTypes = {
};

export {
    DetailedActionTypes,
};